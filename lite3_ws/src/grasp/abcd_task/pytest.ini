[pytest]
# ROS 2 Foxy 的 launch_testing/launch_testing_ros pytest 插件 hook 与新版 pytest（>=8）
# 不兼容，collect 阶段就崩。abcd_task 用不到 launch_testing 能力，全部禁用。
addopts = -p no:launch -p no:launch_ros
testpaths = test
